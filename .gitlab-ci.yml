stages:
  - test

pi_tests:
  stage: test
  script:
    - virtualenv ve
    - . ve/bin/activate
    - pip install -r requirements.txt
    - pip install pytest
    - make schema
    - export PYTHONPATH=`pwd`
    - py.test tests/pi
  tags:
      - raspberrypi
